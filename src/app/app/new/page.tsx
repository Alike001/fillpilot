import { FoundationSurface } from "@/components/foundation-surface";

export default function NewGoalPage() {
  return (
    <FoundationSurface
      eyebrow="Goal setup"
      title="Define one fill. Keep every boundary visible."
      body="This route will collect sell amount, preferred receive, minimum receive, and deadline after the deterministic lifecycle and real connection doctor are verified."
    />
  );
}
